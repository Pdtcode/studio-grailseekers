# Sanity Studio is a single-page app: serve index.html for every route
# (/structure, /structure/order;…, page refreshes) instead of Netlify's 404.
# Real files like /static/*.js still win because this rule isn't forced.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
